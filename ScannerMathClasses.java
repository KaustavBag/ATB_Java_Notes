public class ScannerMathClasses {
    public static void main(String[] args) {
        int a=10;
        int b=20;
        int c=30;
        double result;

        result=Math.sqrt(Math.pow(a,2)+Math.pow(b,2)+Math.abs(c));
        System.out.println(result);
    }
}
